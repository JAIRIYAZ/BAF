// Empty PostCSS config — Tailwind CSS v4 uses the Vite plugin directly,
// no PostCSS plugin needed. This file exists to prevent Vite from walking
// up the directory tree and finding a Tailwind v3 postcss.config.cjs.
export default {
  plugins: {},
};
